<Window x:Class="MediaConverter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MediaConverter"
        mc:Ignorable="d"
        Title="SHE mediaCoverter" Height="700"  MaxWidth="1400">

    <Grid Name="MainGrid" Background="Tomato"><!--#FFF3F3F3-->
        <Grid Name="NavigationGridLeft" Background="White" HorizontalAlignment="Left" Width="250" >
            <StackPanel Name="NavigationLeft">
                <TextBlock Text="mediaConverter" FontSize="30" FontWeight="Light" Padding="5" Background="White" Foreground="#FFA4A3A4"/>
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Wiederherstellen und reparieren" FontSize="16" FontWeight="Thin"/>
                    <!--Ergänzen!!!-->
                    <Button Name="RecoveryButton" 
                            Background="#FFFAFA" 
                            BorderThickness="0" 
                            Content="Original wiederherstellen"
                            Command="{Binding ClickCommand}"
                            Click="button1_Click"/>

                    <Button Background="#FFFAFA" BorderThickness="0" Content="Belegseiten reparieren"/>
                    <TextBlock Text="Konvertieren" FontSize="16" FontWeight="Thin"/>
                    <TextBlock Text="mit DB Eintrag"/>
                    <TextBlock Text="ohne DB Eintrag"/>
                    <TextBlock Text="Ablagecode prüfen" FontSize="16" FontWeight="Thin"/>
                    <TextBlock Text="für Original"/>
                    <TextBlock Text="ohne Original"/>
                </StackPanel>
            </StackPanel>
        </Grid>
        <!--ContainerView-->
        
        <Viewbox Name="ContainerView" HorizontalAlignment="left" VerticalAlignment="Top" Margin="250,0,0,0" />
        
    </Grid>
</Window>