<Window x:Class="MediaConverter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MediaConverter"
        xmlns:self="clr-namespace:MediaConverter.Models"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        mc:Ignorable="d"
        Title="SHE | MediaCoverter" Height="600"  Width="Auto" Background="#FFE8E6E6">

    <Grid Name="MainGrid" Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Name="NavigationLeft" Margin="0,0,10,0" Orientation="Vertical"  >
            <TextBlock Text="Auswahl" Margin="0,0,0,10" 
                       FontSize="15" FontWeight="Normal" Foreground="#FF4F4F4F"/>
            <Border Background="#FFEEEEEE" CornerRadius="5">
                <TreeView Name="trvNodes" ItemsSource="{Binding treeList}"
                          Background="Transparent" BorderThickness="0" Margin="5,5,10,15" >
                    
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="TreeViewMouseClick">
                            <i:InvokeCommandAction Command="{Binding ClickCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>

                    <TreeView.Resources>
                        <HierarchicalDataTemplate DataType="{x:Type self:Tree}" ItemsSource="{Binding Nodes}">
                            
                            <StackPanel Orientation="Horizontal" Margin="0,10,0,10">
                                <Image Source="{Binding Image}"/>
                                <TextBlock Text="{Binding HeadLine}"
                                           Padding="10,0,0,0" FontSize="15" FontWeight="Normal" Foreground="#FF0060CF"/>
                                <TextBlock Text=" (" 
                                           FontSize="15" FontWeight="Normal" Foreground="#FF8D8D8D"/>
                                <TextBlock Text="{Binding Nodes.Count}" 
                                           FontSize="15" FontWeight="Normal" Foreground="#FF8D8D8D" />
                                <TextBlock Text=")" 
                                           FontSize="15" FontWeight="Normal" Foreground="#FF8D8D8D" />
                            </StackPanel>
                            
                        </HierarchicalDataTemplate>
                        <DataTemplate DataType="{x:Type self:TreeNode}">
                            <StackPanel Orientation="Horizontal" Margin="5,0,0,0">

                                <TextBlock Text="{Binding Select}" 
                                           FontSize="13" FontWeight="Normal" Foreground="#FF4F4F4F"/>

                            </StackPanel>
                        </DataTemplate>
                    </TreeView.Resources>
                </TreeView>

            </Border>
        </StackPanel>
        <Border Grid.Column="1" 
                Background="#FFEEEEEE" CornerRadius="5">  
            <!--Hier ist das ContentControl-->
            <ContentControl Grid.Column="1"  Content="{Binding RecoveryOriginalViewModel}"/>
        </Border>


        





        
        

        <!--<Grid Background="White" HorizontalAlignment="Right" VerticalAlignment="Top"  >
            -->
        <!-- overlay with hint text -->
        <!--
            <TextBlock Margin="5,2" MinWidth="50" Text="Suche..." 
                   Foreground="LightSteelBlue" Visibility="{Binding ElementName=txtSearchBox, Path=Text.IsEmpty, Converter={StaticResource MyBoolToVisibilityConverter}}" />
            -->
        <!-- enter term here -->
        <!--
            <TextBox MinWidth="50" Name="txtSearchBox" Background="Transparent" />
        </Grid>-->

    </Grid>
</Window>